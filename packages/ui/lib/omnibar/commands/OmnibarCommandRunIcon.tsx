export const OmnibarCommandRunIcon = ({ size }: { size: number }) => {
  return (
    <div
      className="flex items-center justify-center rounded bg-gray-200 text-xs font-bold text-gray-600 dark:bg-gray-700 dark:text-gray-300"
      style={{ height: `${size}px`, width: `${size}px` }}
    >
      &gt;
    </div>
  )
}
